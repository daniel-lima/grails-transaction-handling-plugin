grails {
    plugin {
        transactionHandling {
            global {
                defaults {
                    // timeout =
                }
            }
            
            programmatic {
                defaults {
                    // isolation =
                    // timeout =
                    // readOnly =
                }
            }
            
            declarative {
                defaults {
                }
            }
        }
    }
}